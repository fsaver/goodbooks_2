<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All groups
    </h1>

    <a href="/groups/new" class="btn btn-block btn-outline-secondary">
      Add a new group
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Name
        </th>

        <th>
          User
        </th>

        <th>
          Review access
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @groups.each do |group| %>
      <tr>
        <!-- Display group.id start -->
        <td>
          <%= group.id %>
        </td>
        <!-- Display group.id end -->

        <!-- Display group.name start -->
        <td>
          <%= group.name %>
        </td>
        <!-- Display group.name end -->

        <!-- Display group.user_id start -->
        <td>
          <%= group.user_id %>
        </td>
        <!-- Display group.user_id end -->

        <!-- Display group.review_access_id start -->
        <td>
          <%= group.review_access.user_id %>
        </td>
        <!-- Display group.review_access_id end -->

        <!-- Display group.created_at start -->
        <td>
          <%= time_ago_in_words(group.created_at) %> ago
        </td>
        <!-- Display group.created_at end -->

        <!-- Display group.updated_at start -->
        <td>
          <%= time_ago_in_words(group.updated_at) %> ago
        </td>
        <!-- Display group.updated_at end -->

        <td>
          <a href="/groups/<%= group.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @groups, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
